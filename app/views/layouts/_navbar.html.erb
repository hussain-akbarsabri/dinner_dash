<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <!-- Container wrapper -->
  <div class="container-fluid">
    <!-- Toggle button -->
    <button
      class="navbar-toggler"
      type="button"
      data-mdb-toggle="collapse"
      data-mdb-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <i class="fas fa-bars"></i>
    </button>
    <!-- Collapsible wrapper -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <!-- Navbar brand -->
      <%= link_to root_path, class: "navbar-brand mt-2 mt-lg-0" do %>
      <%= image_tag 'logo.jpg', class:"height-40px" %>
      <span class="logo-text">Dinner Dash</span>
      <%end%>
      <a  href="#">

        </a>
        <!-- Left links -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'Home', root_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to 'View Products', products_path, class: "nav-link" %>
          </li>
          <%if user_signed_in? && !current_user.admin?%>
            <li class="nav-item">
              <%=
                link_to 'My Orders', orders_path, class: "nav-link"
              %>
            </li>
          <%end%>
          <%if user_signed_in? && current_user.admin?%>
            <li class="nav-item">
              <%=
                link_to 'Categories', categories_path, class: "nav-link"
              %>
            </li>
             <li class="nav-item">
              <%=
                link_to 'Orders Dashboard', orders_dashboard_path, class: "nav-link"
              %>
            </li>
          <%end%>
        </ul>
        <!-- Left links -->
      </div>
      <div class="d-flex align-items-center">
        <%= if !user_signed_in? || (user_signed_in? && !current_user.admin?)
           link_to cart_path, class: "text-reset me-3 text-decoration-none cart-button" do%>
          <i class="fas fa-shopping-cart"></i> View Cart
        <%end
        end%>
        <%if user_signed_in?%>
          <%=
            link_to edit_user_registration_path, class: "btn btn-primary me-2 px-3" do
              current_user.full_name
            end
          %>
          <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-secondary px-3" %>
        <%else%>
          <%= link_to 'Sign in', new_user_session_path, class: "btn btn-outline-primary me-2 px-3"%>
          <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-warning px-3"%>
        <%end%>
      </div>
      <!-- Right elements -->
    </div>
    <!-- Container wrapper -->
  </nav>
  <!-- Navbar -->
